# Tools and Runs API

- Tool run list/detail semantics.
- Approval endpoints (`approve`/`deny`).
- Run trace list/detail semantics.
- Linkage between messages, runs, and tool runs.
- Reasoning visibility toggles in admin surfaces.
