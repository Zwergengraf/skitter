# Docker Compose

- Compose services and profiles overview.
- Build flow for API, admin web, and sandbox images.
- Start flow for DB + API + admin web.
- Optional SearXNG stack from `searxng/docker-compose.yml`.
- Required environment variables for containerized runs.
- Workspace and Docker socket mount behavior.
- Typical compose troubleshooting checks.
