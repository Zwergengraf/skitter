# Quickstart

- Clone repository.
- Create Python virtualenv.
- Install backend dependencies.
- Configure `.env` and `config.yaml`.
- Start PostgreSQL and initialize schema.
- Start API server.
- Connect a client (Discord, TUI, or menubar).
